# Terraform state files
terraform.tfstate
terraform.tfstate.backup

# Terraform plugin and lock files
.terraform/
.terraform.lock.hcl

# Ignore ZIP files if they are generated in the local environment
*.zip
